// Generated by Skyramp v1.3.27 on 2026-06-21 17:17:04.80441825 +0000 UTC m=+659.382545869
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output-dir /home/runner/work/infisical/infisical/tests/skyramp/frontend \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/infisical/infisical/.skyramp/secret_import_item_trace.zip

// Import of required libraries
import { test, expect, getValue, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(446000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    const errors: Error[] = [];
    page.on('pageerror', error => errors.push(error));

    await page.goto("http://localhost:8080/login");
    await page.waitForLoadState('networkidle');
    // Wait for network activities to finish
    await page.waitForTimeout(7500);


    const playwrightRequest0: Record<string, any> = {
        "email": "admin@test.com",
        "password": "Password123!"
    };

    await page.locator("#email").fill(getValue(playwrightRequest0, "email"));
    await page.locator("#current-password").fill(getValue(playwrightRequest0, "password"));
    // Wait to finish any potential hydration
    await page.waitForTimeout(1500);
    const responsePromise0 = page.waitForResponse("**/api/v3/auth/login**");
    await page.getByRole("button", { name: "Continue with Email" }).click();
    const response0 = await responsePromise0;
    expect(response0.status()).toBe(200);

    // Wait for network activities to finish
    await page.waitForTimeout(3000);

    await page.getByRole("button", { name: "Secrets Management" }).click();
    await page.getByRole("button", { name: "Add New Project" }).click();
    await page.locator("#new-project-name").fill("TestProject");
    await page.getByRole("button", { name: "Create Project" }).click();
    // Wait for network activities to finish
    await page.waitForTimeout(3000);
    await page.waitForLoadState('networkidle');
    await expect(page).toHaveURL(/\/projects\/secret-management\//);

    const overviewUrl = page.url();
    const secretsUrl = overviewUrl.replace('/overview', '/secrets/dev');
    await page.goto(secretsUrl);
    await page.waitForLoadState('networkidle');
    await page.locator("main").getByRole("button", { name: "add-folder-or-import" }).waitFor({ state: 'visible', timeout: 10000 });
    await page.waitForSelector("text=Secrets");
    await page.locator("main").getByRole("button", { name: "add-folder-or-import" }).click();
    await page.getByRole("button", { name: "Add Import" }).click();
    await page.locator(".pl-1.css-124rwol").click();
    await page.getByRole("option", { name: "Staging" }).click();
    await page.getByRole("button", { name: "Create Link" }).click();
    // Wait for network activities to finish
    await page.waitForTimeout(3000);

    await expect(page.getByRole("button", { name: "staging / expand delete" })).toContainText("staging");
    await expect(page.getByRole("button", { name: "staging / expand delete" })).toHaveAttribute("data-dragging", "false");
    await expect(page.getByRole("button", { name: "staging / expand delete" })).toHaveAttribute("data-import-id", /.+/);
    await page.getByRole("button", { name: "staging / expand delete" }).click();
    await expect(page.getByText("No secrets found")).toBeVisible();

    expect(errors).toHaveLength(0);
});
