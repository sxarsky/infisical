// Generated by Skyramp v1.3.27 on 2026-06-22 23:19:58.229647592 +0000 UTC m=+713.009287965
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output secret_dashboard_data_state_results_test.spec.ts \
// 		--output-dir /home/runner/work/infisical/infisical/tests/skyramp/frontend \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/infisical/infisical/.skyramp/secret_dashboard_data_state_results_trace.zip

// Import of required libraries
import { test, expect, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

test.use({
    viewport: { width: 1280, height: 900 },
});

test('testUi', async ({ page }) => {
    test.setTimeout(208000);

    const pageErrors: string[] = [];
    page.on('pageerror', (err) => pageErrors.push(err.message));

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto("http://localhost:8080/login");
    await page.locator("#email").fill("admin@test.com");
    await page.locator("#current-password").fill("Password123!");
    await page.getByRole("button", { name: "Continue with Email" }).click();
    await page.waitForLoadState('networkidle');
    await page.goto("http://localhost:8080/organizations/f927b319-87c6-4234-877f-7f02389a0c41/projects/secret-management/eb78dcef-f6b4-41d7-bbb5-6c14ae37e237/secrets/dev");
    await page.waitForLoadState('networkidle');
    await page.locator('[data-testid="secret-sort-toggle"]').waitFor({ state: 'visible', timeout: 10000 });
    // Wait for network activities to finish
    await page.waitForTimeout(5000);

    await expect(page.getByTestId("secret-sort-toggle")).toContainText("Key");
    await expect(page.locator('#dashboard')).toHaveAttribute('data-state', 'results');
    await expect(page).toHaveURL(/secrets\/dev/);
    expect(pageErrors).toHaveLength(0);
});
